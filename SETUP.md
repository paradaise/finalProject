# 🚀 Sound Sentinel - Инструкция по установке и запуску

## 📋 Обзор

**Sound Sentinel** - это система детекции бытовых звуков для людей с нарушением слуха. Система состоит из:

- **Raspberry Pi Zero 2W** - захват аудио
- **ПК с API сервером** - обработка и детекция
- **Веб-приложение** - управление и мониторинг

## 🏗 Архитектура

```
Микрофон (Raspberry Pi) → HTTP API → ML модель → WebSocket → Веб-приложение
```

## 📦 Требования

### Для ПК (API сервер):

- Python 3.9+
- TensorFlow 2.15+
- 8GB+ RAM (для ML модели)
- Windows/Linux/macOS

### Для Raspberry Pi:

- Raspberry Pi Zero 2W
- USB микрофон
- Python 3.9+
- Подключение к той же сети что и ПК

### Для веб-приложения:

- Node.js 18+
- Современный браузер

## 🔧 Установка

### 1. Настройка API сервера (ПК)

```bash
# Клонируем репозиторий
git clone <your-repo-url>
cd finalProject

# Создаем виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем API сервер
python api_server.py
```

API сервер будет доступен на `http://localhost:8000`

### 2. Настройка Raspberry Pi

```bash
# Копируем файлы на Raspberry Pi
scp send_voice.py pi@raspberry-ip:/home/pi/
scp requirements.txt pi@raspberry-ip:/home/pi/

# Устанавливаем зависимости
sudo apt update
sudo apt install python3-pip portaudio19-dev
pip3 install -r requirements.txt

# Настраиваем микрофон
# Проверяем доступные устройства
arecord -l

# Редактируем send_voice.py, меняем IP адрес ПК
nano send_voice.py
# Меняем API_BASE_URL на IP вашего ПК

# Запускаем захват аудио
python3 send_voice.py
```

### 3. Настройка веб-приложения

```bash
# Переходим в папку приложения
cd "Device Management App Design"

# Устанавливаем зависимости
npm install

# Запускаем dev сервер
npm run dev
```

Веб-приложение будет доступно на `http://localhost:5173`

## 🎯 Использование

### 1. Запуск системы

1. **Сначала запустите API сервер на ПК:**

   ```bash
   python api_server.py
   ```

2. **Затем запустите захват аудио на Raspberry Pi:**

   ```bash
   python3 send_voice.py
   ```

3. **Откройте веб-приложение:**
   ```bash
   cd "Device Management App Design"
   npm run dev
   ```
   Откройте `http://localhost:5173` в браузере

### 2. Проверка работы

- В консоли API сервера должны появляться сообщения о загрузке модели
- В консоли Raspberry Pi - сообщения об отправке аудио
- В веб-приложении - устройство должно появиться в списке как "online"

### 3. Настройка персонализации

**Добавление специфических звуков:**

1. В веб-приложении перейдите в устройство → "Специфические звуки"
2. Нажмите "+" для записи нового звука
3. Запишите звук (например, дверной звонок)
4. Система автоматически извлечет MFCC признаки

**Исключение нежелательных звуков:**

1. Перейдите в "Исключенные звуки"
2. Добавьте звук, который нужно игнорировать (например, шум холодильника)

## 🔍 Детектируемые звуки

Система автоматически детектирует следующие звуки:

- **Сигналы тревоги:** пожарная сигнализация, сирена
- **Дверные звуки:** звонок, стук
- **Детские звуки:** плач ребенка
- **Животные:** лай собаки, мяуканье кошки
- **Бытовые:** разбитое стекло, водопровод, бытовая техника
- **Телефонные:** звонок телефона

## 🛠 Настройка конфигурации

### API сервер (`api_server.py`)

```python
# Измените при необходимости
API_HOST = "0.0.0.0"  # Все интерфейсы
API_PORT = 8000
```

### Raspberry Pi (`send_voice.py`)

```python
# Укажите IP адрес вашего ПК с API сервером
API_BASE_URL = "http://192.168.0.61:8000"
DEVICE_NAME = "Living Room Monitor"
```

### Веб-приложение (`src/api/client.ts`)

```typescript
// Укажите IP адрес API сервера
const API_BASE_URL = "http://192.168.0.61:8000";
```

## 🐛 Устранение проблем

### Проблема: Устройство не появляется в веб-приложении

**Решение:**

1. Проверьте, что API сервер запущен и доступен
2. Проверьте IP адрес в `send_voice.py`
3. Проверьте сетевое соединение между Raspberry Pi и ПК

### Проблема: Не детектируются звуки

**Решение:**

1. Убедитесь, что микрофон работает на Raspberry Pi
2. Проверьте уровень громкости в консоли
3. Попробуйте понизить порог уверенности в коде

### Проблема: Ошибки TensorFlow

**Решение:**

1. Убедитесь, что установлена правильная версия TensorFlow
2. Проверьте доступность модели YAMNet
3. Проверьте наличие достаточного количества RAM

## 📊 Мониторинг

### API эндпоинты для мониторинга:

- `GET /` - статус API
- `GET /devices` - список устройств
- `GET /events/{device_id}` - события устройства
- `WebSocket /ws` - реальное время

### Логи:

- API сервер: консольные сообщения
- Raspberry Pi: консольные сообщения
- Веб-приложение: браузерная консоль

## 🚀 Дальнейшее развитие

### Планируемые улучшения:

1. **Облачное развертывание** - вынос API в облако
2. **Мобильное приложение** - нативное iOS/Android
3. **Улучшенная модель** - fine-tuning на бытовых звуках
4. **Push-уведомления** - мгновенные оповещения
5. **Мультиязычность** - поддержка разных языков

### Возможные доработки:

- Добавление видео-мониторинга
- Интеграция с умным домом
- Обучение на пользовательских данных
- Статистика и аналитика

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи всех компонентов
2. Убедитесь в сетевой связности
3. Проверьте версии зависимостей
4. Создайте Issue в репозитории

---

**Готово к использованию! 🎉**

# Sound Sentinel MVP - Инструкция по установке

## 🚀 Быстрый старт

### 1. API Сервер (ПК)

```bash
cd mvp_sound_sentinel/api_server
pip install -r requirements.txt
python main.py
```

Сервер запустится на `http://localhost:8000`

### 2. Raspberry Pi Клиент

```bash
cd mvp_sound_sentinel/raspberry_pi
pip install -r requirements.txt
python audio_client.py
```

### 3. Мобильное приложение

```bash
cd mvp_sound_sentinel/mobile_app
npm install
npm run dev
```

Приложение откроется на `http://localhost:5173`

## 📋 Требования

### API Сервер

- Python 3.8+
- TensorFlow 2.15+
- 4GB RAM минимум
- Интернет для загрузки модели

### Raspberry Pi

- Python 3.7+
- PyAudio (может потребоваться `sudo apt-get install portaudio19-dev`)
- Микрофон USB или наушники с микрофоном

### Мобильное приложение

- Node.js 16+
- Современный браузер

## 🔧 Конфигурация

### Изменение IP адреса API сервера

В файлах:

- `raspberry_pi/audio_client.py`: `API_SERVER_URL = "http://ВАШ_IP:8000"`
- `mobile_app/src/api/client.ts`: `const API_BASE_URL = 'http://ВАШ_IP:8000';`

## 🗄️ База данных

SQLite база создается автоматически:

- `sound_sentinel.db` - основной файл БД
- Можно открыть через DB Browser for SQLite

## 🎯 Проверка работы

1. Запустите API сервер
2. Запустите клиент на Raspberry Pi
3. Откройте мобильное приложение
4. Сделайте звук рядом с микрофоном Raspberry Pi
5. В приложении должна появиться детекция звука

## 🐛 Частые проблемы

### PyAudio не устанавливается на Raspberry Pi

```bash
sudo apt-get update
sudo apt-get install portaudio19-dev python3-pyaudio
```

### TensorFlow не загружается

Установите правильную версию:

```bash
pip install tensorflow==2.15.0
```

### Мобильное приложение не подключается к API

Проверьте IP адрес и防火墙 настройки на ПК.

## 📊 Структура проекта

```
mvp_sound_sentinel/
├── api_server/          # FastAPI сервер с YAMNet
├── raspberry_pi/        # Клиент для записи аудио
├── mobile_app/         # React приложение
└── sound_sentinel.db   # SQLite база данных
```

## 🎵 Поддерживаемые звуки

YAMNet распознает 521 класс звуков включая:

- Животные (dog, cat, bird)
- Бытовые (doorbell, phone, alarm)
- Транспорт (car, siren, engine)
- Человеческие (speech, cough, laugh)
- Музыкальные инструменты

## 🔄 Реальное время

- WebSocket обновления каждые 2 секунды
- Автоматическое переподключение
- История детекций сохраняется в БД
